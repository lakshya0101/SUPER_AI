playwright>=1.44.0
azure-storage-blob>=12.19.0
pypdf>=4.2.0
PyMuPDF>=1.24.0
openpyxl>=3.1.2
python-dotenv>=1.0.1
openai>=1.35.0
Pillow>=10.3.0
pytesseract>=0.3.10
pytest>=8.2.0

# Notes:
# - After installing Playwright, run: python -m playwright install
# - This project launches installed Microsoft Edge, so Edge must be installed on the machine.
# - OCR fallback needs the external Tesseract OCR engine installed and available on PATH.
